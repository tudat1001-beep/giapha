# ─── AncestorTree — Docker Environment ──────────────────────────────────────
# Copy this file to .env and fill in your Supabase Cloud credentials.
# Usage: docker compose --env-file .env up -d
#
# DO NOT commit .env to git — it contains secrets.

# ─── Supabase Cloud (required) ──────────────────────────────────────────────
# Get from: https://supabase.com/dashboard → your project → Settings → API
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here   # SECRET — server-only

# ─── App port (optional, default 4000) ──────────────────────────────────────
APP_PORT=4000

# ─── Clan customization (optional) ──────────────────────────────────────────
# NEXT_PUBLIC_CLAN_NAME=Họ Nguyễn
# NEXT_PUBLIC_CLAN_FULL_NAME=Họ Nguyễn làng ABC, Quảng Nam

# ─── Debug / Logging (optional, disable in production) ──────────────────────
# MIDDLEWARE_LOG=true   # Enables structured JSON logs in middleware (docker logs -f app)
# DEBUG_AUTH=true       # Enables GET /api/debug/auth inspection endpoint
